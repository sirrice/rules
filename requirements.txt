flask==3.1.3
# Pinned to match the version that wrote sample.db / annotations.db.
# DuckDB refuses to open a database created by a newer minor version, so keep
# this in sync with the host DuckDB if you regenerate the .db files.
duckdb==1.5.2
